// lib/renderReel.js
// Replace this with your real renderer. This is a stub so routes compile.
export async function renderReelToMp4({ reel, renderParams }) {
  // Throw for now so you notice if you call it before wiring your renderer.
  // When ready, return { mp4Buffer, posterJpegBuffer, durationSeconds, checksum }.
  throw new Error('renderReelToMp4 not implemented');
}
