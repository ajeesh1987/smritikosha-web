// /api/_lib/renderReel.js
// You will wire this to your real renderer
export async function renderReelToMp4({ reel, renderParams }) {
  // Return { mp4Buffer, posterJpegBuffer, durationSeconds, checksum }
  throw new Error('Renderer not connected');
}
